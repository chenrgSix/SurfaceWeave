# @package-first/preferences

Scoped, structured Preference Patches, conflict detection, and explicit
migration over generated Dynamic UI Surfaces.

```sh
npm install @package-first/core@next @package-first/storage@next @package-first/preferences@next
```
